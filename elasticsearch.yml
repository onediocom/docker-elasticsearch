path:
  data: /data/data
  logs: /data/log

discovery:
  type: ec2
  ec2:
    groups: ElasticSearchGroup

plugin:
  mandatory: cloud-aws

cluster:
  name: ElasticSearchGroup

http.cors.enabled: true
network.publish_host: _ec2_
cloud.aws.region: eu-west-1
threadpool.bulk.queue_size: 20000
